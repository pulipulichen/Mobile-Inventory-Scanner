---
description: scan 與 print 的純前端 Vue 開發、依賴與 Podman 編譯規範
alwaysApply: true
---

# Frontend Rules

本規則適用於 `scan/` 與 `print/`。

若本規則與其他從舊專案沿用的 Django、AdminLTE、後端或前端規則衝突，`scan/` 與 `print/` 以本規則為準。

## 1. 架構原則

- `scan/` 與 `print/` 都必須是 **純前端靜態網頁**。
- 正式部署後只需要靜態檔案伺服器，不需要 Node.js runtime、Python、PHP、Django、Express 或其他自建後端。
- Node.js 只存在於開發 / 編譯階段。
- 前端與 Google Sheet / Apps Script 直接通訊，不建立中介 API server。
- 不可把 Google Client Secret、service account key、私人憑證或其他 secret 放入前端 bundle。

## 2. 固定技術棧

兩個 App 統一使用：

- Vue 3
- Vuetify 4.x：`scan` 與 `print` 共用的 UI framework
- `vue-i18n`
- Composition API
- `<script setup lang="ts">`
- TypeScript
- Vite
- `vite-plugin-vuetify`：Vite 編譯時按需載入 Vuetify 元件
- SCSS / Sass
- npm

第一版不加入：

- Vue Router：兩個 App 都是單頁工具，不需要路由。
- Pinia：狀態量小，優先使用 composable 與 Vue reactive/ref。
- SSR / Nuxt：不需要伺服器端 rendering。
- 第二套 UI framework：所有共用 UI 元件統一使用 Vuetify，不混用
  Bootstrap、PrimeVue、Quasar、Element Plus 或其他 UI framework。

Vuetify 只在建置階段由 Vite 打包進靜態資源；正式環境不需要 Node.js
runtime、Vuetify server 或其他自建後端。使用元件時應透過
`vite-plugin-vuetify` 按需載入，避免把未使用的元件全部加入 bundle。

## 3. 專案結構

兩個 App 都各自維持獨立 Vite 專案：

```text
scan/
├── index.html
├── package.json
├── package-lock.json
├── vite.config.ts
├── tsconfig.json
├── public/
└── src/
    ├── App.vue
    ├── main.ts
    ├── assets/
    ├── components/
    ├── composables/
    ├── services/
    ├── styles/
    ├── types/
    └── utils/

print/
└── 同樣結構
```

共用程式碼在真的出現重複需求後才抽共用 package；第一版不要過早建立 monorepo framework。

## 4. Vue / TypeScript

- Vue component 使用 `<script setup lang="ts">`。
- 業務資料結構必須定義 TypeScript type / interface。
- API 回傳不可直接假設型別正確；在 service 邊界做基本驗證與錯誤轉換。
- Component 以 UI 呈現與互動為主；網路、localStorage、QR decode / encode 邏輯拆到 `services/`、`composables/` 或 `utils/`。
- 不使用 `any` 規避型別問題；若第三方 library 型別不足，建立明確 wrapper。

## 5. SCSS

- 全域 SCSS 放 `src/styles/`。
- Component-specific 樣式優先使用 `<style scoped lang="scss">`。
- Vuetify 的 theme、SCSS 變數與元件設定集中管理；不可在各 component
  重複覆寫同一組設計 token。
- PDF 版面尺寸涉及實體輸出時，使用 `mm` / `pt`，不要以畫面 pixel 假裝實際尺寸。
- `print` 預設桌面優先但必須支援平板與手機 RWD。
- 手機掃描頁採 mobile-first。

## 6. localStorage

所有規格要求保存的輸入與設定，都要透過集中 wrapper / composable 存取，不要在各 component 到處直接呼叫 `localStorage`。

key 使用固定 prefix：

```text
mis.scan.*
mis.print.*
```

例如：

```text
mis.scan.apps_script_url
mis.scan.location
mis.scan.location_history
mis.print.google_sheet_url
mis.print.qr_size_mm
```

資料結構若可能變更，應保留 schema/version 的升級空間。

## 7. scan 專用技術決策

- 必須支援 Android Chrome 與 iPhone Safari。
- 第一版使用 `MediaDevices.getUserMedia()` 持續取得後置鏡頭影像，並提供
  `<input type="file" accept="image/*">` 作為拍照 / 讀取相片的替代入口；拍照入口加
  `capture="environment"`。
- 即時掃描以節流的相機影格迴圈交給 QR decoder，停止掃描時必須釋放所有
  `MediaStream` tracks。
- QR Code 辨識全部在瀏覽器本機執行，不上傳照片或相機影像。
- QR 解碼固定優先使用 `@undecaf/zbar-wasm`，原因是可在瀏覽器處理
  `ImageData` 並一次回傳多個 barcode / QR Code。
- WASM 檔案應跟著 Vite build 部署，不依賴第三方 CDN 才能掃描。
- 同一影格、同一張圖片與同一掃描 session 中重複辨識出的 ID，必須去重後才呼叫
  Apps Script。
- `scan` 必須是 PWA；使用 `vite-plugin-pwa` 管理 manifest / service worker。
- PWA 快取只保證 App shell / 靜態資源可啟動；第一版不做離線待送 queue。

## 8. print 專用技術決策

- QR Code 產生優先使用 npm `qrcode` 套件。
- 預覽使用 SVG QR Code；PDF 使用 `qrcode` 的 QR matrix 搭配 `pdf-lib` 繪製向量模組。
- PDF 在瀏覽器本機由 `pdf-lib` 產生並下載，不使用 `window.print()` 或瀏覽器另存 PDF。
- Google Sheet 讀取使用公開 CSV export URL 搭配瀏覽器 `fetch`。
- 使用者輸入 Google Sheet URL，前端解析 Spreadsheet ID，再下載
  `https://docs.google.com/spreadsheets/d/{spreadsheet_id}/export?format=csv`。
- Sheet 必須能由瀏覽器直接下載 CSV；不使用 Google OAuth、Google Identity
  Services 或 Google Sheets API。

## 9. 網路層

- 外部 API 呼叫集中在 `src/services/`。
- UI component 不直接散落 `fetch()`。
- 所有 request 都要有 loading、success、error 狀態。
- `scan` 不得因單筆 Apps Script 失敗而停止同張圖片其他 ID。
- 不建立自建 CORS proxy；Apps Script / Google API 必須設計成瀏覽器可直接呼叫。

## 10. i18n

- `scan` 與 `print` 統一使用 `vue-i18n`；因為兩個 App 都是 Vue 3 應用程式，使用 Vue 生態系的 i18n 套件比自製 `i18n.js` 更能整合 Composition API、插值、複數與型別檢查。
- 每個獨立 App 都要在自己的 `package.json` 與 `package-lock.json` 管理 `vue-i18n`；翻譯資源隨 Vite bundle 部署，不使用 CDN、執行期遠端字典或自建後端。
- 以 Composition API 模式初始化 `createI18n({ legacy: false })`，元件使用 `useI18n({ useScope: "global" })`；初始化集中在 `src/i18n/index.ts`。
- 翻譯檔集中放在 `src/i18n/messages/`，例如 `zh_tw.ts` 與 `en.ts`。檔名遵循專案的 `snake_case`，locale 值使用標準 BCP 47 `zh-TW` 與 `en`。
- 預設語系為 `zh-TW`，fallback 語系為 `en`。初始化優先順序為：App 專用 localStorage 語系、瀏覽器語系、預設語系；語系切換必須立即更新靜態文字、動態文字與 `html[lang]`。
- 語系設定必須透過集中 wrapper / composable 保存，不可在元件直接呼叫 `localStorage`；使用 `mis.scan.locale` 與 `mis.print.locale`。
- 所有使用者可見文字都必須使用翻譯 key，包括按鈕、錯誤、loading、成功訊息、ARIA label、live region 與 PDF 標籤文字。翻譯 key 使用具命名空間的 `snake_case`，例如 `scan.capture_qr_code`。
- 各語系必須維持相同的 key schema，缺少 key 應在測試或 CI 中被發現；不可把英文原文或 translation key 當成正式 UI fallback。
- 使用 `vue-i18n` 的插值與複數功能處理變數文字，不以字串串接組合句子；日期與數字依 locale 使用 `Intl` 或 `vue-i18n` 格式化。
- Apps Script / Google API 的英文 `message` 與 `error` code 是資料契約；前端依 `error` code 使用 `t()` 顯示本地化訊息，不把翻譯後文字送回 API。ID、location 等使用者資料不可翻譯。
- 語系選擇器必須使用有 label 的原生 `<select>`，支援鍵盤操作；重要狀態通知需沿用可存取的 live region，切換語系後也必須更新其文字。

## 11. Podman 開發與編譯

**主機不要求安裝 Node.js 或 npm。**

Node / npm 一律從根目錄的 `Containerfile.frontend` 建立 Podman image，並透過 `frontend.sh` 執行。

統一命令：

```bash
./frontend.sh image
./frontend.sh install scan
./frontend.sh install print
./frontend.sh dev scan
./frontend.sh dev print
./frontend.sh build scan
./frontend.sh build print
```

規則：

- 不在 README 教使用者直接執行 host `npm`。
- 不把 `node_modules/`、`dist/`、`.vite/` commit 進 Git。
- `package-lock.json` 必須 commit，確保 build 可重現。
- 新增 / 更新 npm dependency 後同步更新 lockfile。
- 正式部署內容只取各 App 的 `dist/`。

## 12. 完成前檢查

修改 `scan/` 或 `print/` 前端後至少執行：

```bash
./frontend.sh build scan
./frontend.sh build print
```

若只修改其中一個 App，至少 build 該 App。

不可在 build 失敗的情況下宣告功能完成。
